# P7 — Digest and ops

- `harness digest` → scan/classify/act/report; paid-URL guard via config.
- Docs: `docs/ops.md` (Task Scheduler + VTA writer / laptop query).
- Acceptance: `python -m pytest -q -k test_p7_`
