# P4 — Extract and classify

- Docling/OCR wraps with offline stubs; LiteLLM client pinned to local proxy URL.
- Correction-rules-first router; naming via shared helper.
- Acceptance: `python -m pytest -q -k test_p4_` (passed).
