# Every unique file gets a home

`09_Archive` and `Vince Backup` move into VincePersonal. The Organizer classifies each unique file into `00`–`06`. Byte-identical copies are journaled as skips and are not classified again. First-class is findability of unique Documents, not a Spark call per backup copy. Leaving those trees on Petra OneDrive is rejected (ADR 0001). Parking the whole archive unread is rejected because Vince leaned to file them and the effort is not shy.
