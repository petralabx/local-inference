# Readable names; metadata lives in the journal

A Document’s filename is a human-readable title. Date, type, and version live in the journal and the Vince Node projection. The coded `YYYY-MM-DD_PREFIX_…_vNN` law is retired. The old enforcer scored 27–38% because it punished people and code trees. The Correction Log is the lesson, not a regex on the name.
