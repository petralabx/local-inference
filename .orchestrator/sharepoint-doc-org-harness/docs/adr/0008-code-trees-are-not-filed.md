# Code trees are not filed

When draining Petra OneDrive `07_Admin` and any later swamp, the Organizer skips git repos, `node_modules`, `.venv`, and `agentic-swarm` trees. SharePoint is not a git host. The old name-enforcer failed by scoring those trees.
