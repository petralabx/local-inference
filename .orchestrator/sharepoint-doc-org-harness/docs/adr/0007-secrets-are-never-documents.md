# Secrets are never Documents

Keys, tokens, and credential dumps do not become Documents. They stay in the secrets manager or a local vault the Organizer skips. SharePoint plus Brain ingest is how the July graph-secret copies spread. One corpus does not mean the cloud holds `.aws` files.
