# Correction Log is the lesson

Karpathy’s rule: do not put memory in the model. Keep a boring, append-only, greppable dataset of (situation → Vince override). That Correction Log sits next to the journal. The Vince Node and any compiler ingest a projection. If Brain is down, the Organizer still files from the log. We rejected “Brain is the only memory” because filing must survive a down node, and we rejected hidden chat memory because you cannot judge what you cannot read.
