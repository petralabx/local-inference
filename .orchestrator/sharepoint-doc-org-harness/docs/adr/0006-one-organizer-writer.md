# One Organizer writer

The smart set is not more agents. It is one Organizer that files, a Correction Log it learns from, and Vince as judge on diffs. COS and MC Ask may query the Vince Node when Vince evokes them. They must not move Documents. A Learner or Auditor that can write would split the corpus again. Health reports are a function of the Organizer, not a second mover.
