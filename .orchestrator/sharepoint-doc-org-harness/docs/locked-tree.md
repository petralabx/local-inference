# Locked tree (shared understanding)

Confirm this before smoke, redirect, or drain.

## Outcomes

- The Organizer files. Vince does not hand-file after cleanup.
- Findability is the proof. “Where did it go?” is a query.
- VincePersonal is the only store. One visible Save Path.
- One Organizer writer. COS/MC Ask query only.
- Correction Log is the lesson. Journal wins unless Vince judges.
- Vince Node is private inbound. Vince may toggle it beside company PLX in COS.
- Secrets never become Documents. Code trees are not filed.
- After this build, tokens go through Dell LiteLLM to Spark `local-driver` / `local-coder`.

## Documents

- Known folders on VTA and taylorvalton redirect into `00_Inbox/_from_desktop|_from_documents|_from_downloads`. Same method on every machine. No second Desktop.
- The Organizer applies `YYYY-MM-DD_PREFIX_Readable Title_vNN` at digest and at relabel of already-filed homes. Vince does not type that law. Prefix, type, date, and version live in the Document ledger. SharePoint Title and Party/Prefix/Home are a projection of that ledger (ADR 0025). The Vince Node holds a projection.
- Petra OneDrive maps per ADR 0016. Unique archive and backup files are classified. Hash copies are skipped.
- Leftover VincePersonal roots fold or park per ADR 0015.
- After a Petra source is empty, remove or hide it. Do not leave a second tree.
- No auto-archive. No inbox ceiling.

## Mail

- Live in the same era as documents.
- First pass: last 90 days of attachments. Then the rest of the mailbox.
- Outlook folders move only when a Correction says so.

## Sequence

1. Five-file document smoke.
2. Redirect both machines to the one method (taylorvalton: before the next save on that machine).
3. Full drain of the mapped corpus.
4. Once-daily Organizer until proven, then 06:00 / 10:00 / 14:00 / 18:00 America/Toronto.
